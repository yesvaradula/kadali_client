import React from 'react'
import Loading from 'react-fullscreen-loading'

const FullScreenLoader = (props) => {
  return <Loading loading background="#313336cc" loaderColor="#fff" />
}

export default FullScreenLoader
